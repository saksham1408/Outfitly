name: outfitly
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.7.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  go_router: ^17.0.0
  supabase_flutter: ^2.12.2
  google_maps_flutter: ^2.14.0
  google_fonts: ^6.3.2
  http: ^1.6.0
  image_picker: ^1.1.2
  google_generative_ai: ^0.4.7
  flutter_dotenv: ^5.2.1
  camera: ^0.11.0+2
  table_calendar: ^3.1.3
  flutter_local_notifications: ^17.2.3
  timezone: ^0.9.4
  # AR Virtual Try-On: capture the merged camera + garment overlay
  # and push it straight into the native share sheet.
  screenshot: ^3.0.0
  share_plus: ^10.1.4
  path_provider: ^2.1.5
  # Live delivery-location detection on the home header: geolocator
  # hands us the device coordinates (with permission), geocoding turns
  # them into a human "Mumbai 400001" string, and shared_preferences
  # caches the last resolved address so cold launches don't flicker.
  geolocator: ^13.0.1
  geocoding: ^3.0.0
  shared_preferences: ^2.3.3
  # Client-generated UUIDs — used for wardrobe item ids (so the
  # storage path can be resolved before the Postgres row exists) and
  # for the saved-address cache keys.
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  # Generates every iOS + Android launcher icon size from a single
  # source PNG. Run with: dart run flutter_launcher_icons
  flutter_launcher_icons: ^0.14.1
  # Generates the native pre-frame splash (shown for a few frames
  # before the Flutter engine boots) so there's no white flash between
  # OS launch and our animated Dart SplashScreen taking over. Run with:
  #   dart run flutter_native_splash:create
  flutter_native_splash: ^2.4.1

# ── Launcher-icon generation ──
# Source image: assets/branding/vastrahub_icon.png (1024x1024, square).
# iOS requires an opaque icon (no alpha), which is why remove_alpha_ios:
# true flattens any transparency onto the configured background.
flutter_launcher_icons:
  image_path: "assets/branding/vastrahub_icon.png"
  android: true
  ios: true
  remove_alpha_ios: true
  background_color_ios: "#FFFFFF"  # white canvas matches the logo artwork

# ── Native pre-frame splash ──
# Shown for the handful of frames between the OS launch and the Flutter
# engine being ready to paint. Keeping the background (#FFFFFF) and the
# logo identical to the animated Dart SplashScreen means the two stages
# read as one continuous moment — no jarring colour or logo swap.
#
# Android 12+ uses a separate splash spec (the OS itself enforces the
# layout), so we configure it explicitly with the same assets.
#
# Regenerate with:  dart run flutter_native_splash:create
flutter_native_splash:
  color: "#FFFFFF"
  image: assets/branding/vastrahub_icon.png
  android_12:
    color: "#FFFFFF"
    image: assets/branding/vastrahub_icon.png

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - .env
    - assets/branding/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
